package org.sda.java19;

/**
 * 2. Warehouse
 * a. User should be able to: add, display all of the details, update, delete an item
 * b. Use composition and collections (The warehouse has products/items)
 * c. Add possibility to display summaries, like sum of all of the products, their prices.
 * d. *Add possibility to update number of items in a specific way, e.g.:
 * “pliers:30”
 * “scissors:+4”
 *
 * @author Marko
 */
public class Main {
    public static void main(String[] args) {

        /*
        1. Create Warehouse, Product, ProductCategory classes (food, fruits, veggeies, shoes..) (ENUM), Currency(EUR, USD, ...) (ENUM) - models package
        2. Warehouse : name, List of products, address, isActive
        3. Product: name, pricePerItem, quantity, productCategory, currency, isAvailable
        4. Create Warehouse object, Before create list of products, then assign product to warehouse object
        5. In main method, Switch case for varoius operations
        6. for each operation, call service method, call service method and do the operation



                 */
    }
}